# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TraverseDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod ‘ReactiveObjC’
  # Pods for TraverseDemo

  target 'TraverseDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TraverseDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
